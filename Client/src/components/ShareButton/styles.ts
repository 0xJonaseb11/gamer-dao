import styled from 'styled-components';

export const StyledShareContent = styled.div`
  display: grid;
  gap: 8px;

  .share-socials-title {
    margin: 16px 0 4px;
  }
  
  .share-socials {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
  }
`;
