import { HardhatRuntimeEnvironment } from "hardhat/types";
import { Logger } from "../logger/logger";
export declare class Deployer {
  private hre;
  private skipVerificationErrors;
  private reporter;
  private deployer;
  private verifier;
  constructor(hre: HardhatRuntimeEnvironment, skipVerificationErrors: string[]);
  startMigration(logger: Logger, verify: boolean, confirmations: number, attempts: number): Promise<void>;
  link(Library: any, ...Contracts: any): Promise<void>;
  deploy(Instance: any, ...args: any): Promise<any>;
  deployProxy(Instance: any, implementationAddress: string, ...args: any): Promise<any>;
  finishMigration(logger: Logger): Promise<void>;
}
//# sourceMappingURL=deployer.d.ts.map
