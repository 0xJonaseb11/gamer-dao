import { HardhatRuntimeEnvironment } from "hardhat/types";
export declare class Migrations {
  private hre;
  private from;
  private to;
  private only;
  private skip;
  private verify;
  private confirmations;
  private skipVerificationErrors;
  private attempts;
  private pathToMigration;
  constructor(
    hre: HardhatRuntimeEnvironment,
    from: number,
    to: number,
    only: number,
    skip: number,
    verify: boolean,
    confirmations: number,
    skipVerificationErrors: string[],
    attempts: number,
    pathToMigration: string
  );
  migrate(): Promise<void>;
  migrateVerify(): Promise<void>;
  private getMigrationFiles;
  private getParams;
  private resolvePathToFile;
}
//# sourceMappingURL=migrations.d.ts.map
