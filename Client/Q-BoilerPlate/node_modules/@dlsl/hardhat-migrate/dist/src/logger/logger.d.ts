import { HardhatRuntimeEnvironment } from "hardhat/types";
export declare class Logger {
  private hre;
  nativeSymbol: string;
  private totalCost;
  private totalTransactions;
  constructor(hre: HardhatRuntimeEnvironment);
  init(): Promise<void>;
  logTransaction(tx: any, name: any, printLogs?: boolean): void;
  summary(): void;
  logContracts(...contracts: any): void;
  log(...args: any[]): void;
  private getNativeSymbol;
  private filterChainsByChainId;
  private getRecords;
  private underline;
}
//# sourceMappingURL=logger.d.ts.map
