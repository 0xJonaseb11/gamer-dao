import { BaseContract, Signer, providers } from "ethers";
import { BaseContractInstance } from "./BaseContractInstance";
export declare class SystemContractInstance<T extends BaseContract> extends BaseContractInstance<T> {
    readonly address: string;
    /**
     * Constructor
     * @param signer ethers signer
     * @param abiFile abi json file
     * @param address contract address
     */
    constructor(signer: Signer | providers.Provider, abiFile: string, address: string);
}
