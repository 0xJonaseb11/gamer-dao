import { ContractTransaction, providers, Signer } from "ethers";
import { SystemContractInstance } from "../SystemContractInstance";
import { TxOptions } from "../../types";
import { DAOMemberStorage } from "../../ethers-contracts";
export declare class DAOMemberStorageInstance extends SystemContractInstance<DAOMemberStorage> {
    constructor(signer: Signer | providers.Provider, address: string);
    addMember(member: string, txOptions?: TxOptions): Promise<ContractTransaction>;
    removeMember(member: string, txOptions?: TxOptions): Promise<ContractTransaction>;
    removeMembers(members: string[], txOptions?: TxOptions): Promise<ContractTransaction>;
}
